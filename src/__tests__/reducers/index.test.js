import * as index from '../../reducers/index';

describe('binary search', () => {
  it('should return 0, given empty list', () => {});
});

describe('sort', () => {
  it('should return empty list, given empty list', () => {});
});

describe('edit object', () => {
  it('', () => {});
});

describe('update object', () => {
  it('', () => {});
});
